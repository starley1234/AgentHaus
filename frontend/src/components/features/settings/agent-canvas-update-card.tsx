/**
 * Version/update card for the settings navigation surfaces.
 *
 * Disabled: this is a personal fork (starley1234/OpenHands) built from source,
 * so the upstream npm/ghcr "latest" check doesn't apply. The component is kept
 * as a no-op stub so the three settings surfaces that mount it stay untouched;
 * it renders nothing.
 */
export function AgentCanvasUpdateCard() {
  return null;
}
