# Спецификация (BOM) из PDF

Разбирает PDF со спецификацией (ведомость материалов) постранично через vision-модель в иерархический JSON + сводку.

Автоматически созданный сервис. Ввод: файл (фото и т.п.).
